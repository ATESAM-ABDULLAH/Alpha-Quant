# Alpha-Quant
System that ingests historical stock market data (and optionally news or social media sentiment), preprocesses it using Hadoop, trains a deep learning model for time-series forecasting, and deploys the predictor using Docker containers. The system can serve real-time predictions or insights to users via an API.
